# ความปลอดภัยของระบบ

บันทึกผลการตรวจความปลอดภัยทั้งเว็บ และสิ่งที่ทำไปแล้ว/ยังไม่ได้ทำ

**บริบท**: ระบบนี้ดำเนินการโดยพรรคการเมืองในประเทศไทย และเก็บ**ข้อมูลสุขภาพ + เลขบัตรประชาชน**
ของประชาชนที่มายืมอุปกรณ์ นั่นแปลว่าคนที่อยากโจมตีไม่ได้มีแค่คนหาเงิน แต่มีคนที่อยากได้
"รายชื่อคนที่ติดต่อพรรคนี้" ด้วย — ซึ่งเป็นข้อมูลที่ทำร้ายคนได้จริงถ้าหลุด

เอกสารนี้เขียนเพื่อให้คนที่ดูแลระบบต่อรู้ว่า **อะไรกันไว้แล้ว อะไรยังเปิดอยู่** และ
**อะไรที่โค้ดทำให้ไม่ได้ ต้องไปตั้งค่าใน Vercel/Neon เอง**

---

## สรุปสถานะ 14 ข้อ

| # | เรื่อง | สถานะ |
|---|---|---|
| 1 | session ไม่ตายเมื่อปิดบัญชี/เปลี่ยนรหัส | ✅ แก้แล้ว |
| 2 | ฟอร์มสาธารณะเขียนทับข้อมูลผู้ยืมด้วยเลขบัตรที่ขโมยมาได้ | ✅ แก้แล้ว |
| 3 | รหัสผ่านอย่างเดียว ฟิชชิ่งได้ | ✅ แก้แล้ว — บังคับพาสคีย์ |
| 4 | rate limit ต่อ username ทำให้ล็อกแอดมินได้ | ✅ แก้แล้ว |
| 5 | ดึงข้อมูลทั้งฐานได้ด้วย `?limit=999999` | ✅ แก้แล้ว |
| 6 | ไม่มี log ว่าใครเปิดดูข้อมูลส่วนบุคคลบ้าง | ✅ แก้แล้ว |
| 7 | ไม่มี security header / CSP | ✅ แก้แล้ว |
| 8 | อัปโหลดไฟล์อะไรก็ได้ที่อ้างว่าเป็นรูป + EXIF พิกัดติดไปด้วย | ✅ แก้แล้ว |
| 9 | รูปบัตรเก็บเป็น public blob เดา URL ได้ | ✅ แก้แล้ว |
| 10 | dependency มีช่องโหว่ค้างอยู่ | ✅ แก้แล้ว |
| 11 | นโยบายรหัสผ่านหลวม | ✅ แก้แล้ว |
| 12 | บอทถล่มฟอร์มสาธารณะ | ✅ แก้แล้ว — Turnstile (ต้องใส่ key เอง) |
| 13 | ไม่มีระบบลบข้อมูลตามกำหนด (PDPA บอกว่าเก็บ 2 ปี) | ✅ แก้แล้ว |
| 14 | สิ่งที่ต้องตั้งค่าเองใน Vercel/Neon | ⬜ **ต้องทำด้วยมือ** |

---

## 1. Session ตายจริงเมื่อถูกถอนสิทธิ์

**เดิม**: cookie ของ iron-session ถือ role และชื่อไว้ในตัวมันเอง เซิร์ฟเวอร์ไม่เคยถามฐานข้อมูล
ซ้ำเลย แปลว่าปิดบัญชีเจ้าหน้าที่ที่ลาออกไปแล้ว เขายังใช้ระบบต่อได้จนกว่า cookie จะหมดอายุ
และเจ้าหน้าที่ที่ถูกลดสิทธิ์จาก admin ก็ยังเป็น admin ต่อ

**ตอนนี้**: `lib/auth.ts` → `requireActiveUser()` อ่านแถวผู้ใช้จริงทุก request แล้วเช็คสองอย่าง
คือ `active` และ `sessionVersion` ที่ต้องตรงกับตัวเลขใน cookie
· `sessionVersion` ถูกดันขึ้นเมื่อ **ปิดบัญชี · เปลี่ยนรหัสผ่าน · เปลี่ยน role · รีเซ็ตพาสคีย์**
· แปลว่า "ตัดทุกเครื่องที่ยังเปิดค้างไว้" เป็นการเพิ่มเลขหนึ่งครั้ง ไม่ต้องไล่ลบ session

## 2. ฟอร์มสาธารณะไม่เขียนทับข้อมูลผู้ยืมอีกแล้ว

**เดิม**: `POST /api/requests` ถ้าเจอเลขบัตรที่มีในระบบแล้ว จะ **update** ชื่อ ที่อยู่ เบอร์
ตามที่ฟอร์มส่งมา — โดยไม่ได้พิสูจน์อะไรเลยว่าคนกรอกคือเจ้าของเลขบัตรนั้น
ใครก็ตามที่รู้เลขบัตรของคนอื่น (ซึ่งหลุดกันเกลื่อน) เปลี่ยนที่อยู่และเบอร์ของคนนั้นในระบบได้

**ตอนนี้**: คำขอที่ส่งเข้ามา**ไม่แตะแถวผู้ยืมเลย** ข้อมูลที่กรอกถูกเก็บไว้ที่คำขอต่างหาก
หน้าเจ้าหน้าที่ (`/staff/requests/[id]`) จะ**ชี้ให้เห็นว่าช่องไหนไม่ตรงกับที่มีในระบบ**
แล้วให้เจ้าหน้าที่กดรับข้อมูลใหม่เอง ผ่าน `POST /api/requests/[id]/adopt-contact` ซึ่งบันทึก audit

## 3. พาสคีย์แทนรหัสผ่าน

**ภัยจริงคือฟิชชิ่ง** ไม่ใช่การเดารหัส หน้า login ปลอมที่ก๊อปหน้าตามาเป๊ะ ๆ แล้วส่งลิงก์ทาง LINE
ได้ผลกับรหัสผ่านเสมอ และได้ผลกับ OTP เกือบเสมอ (ส่งต่อ code ภายใน 30 วินาที)
**พาสคีย์ผูกกับชื่อโดเมนจริง** เว็บปลอมจึงใช้ไม่ได้ ต่อให้คนกดยืนยันไปแล้ว

- login ครั้งแรกด้วยรหัสผ่านที่แอดมินให้ → **บังคับสร้างพาสคีย์ก่อน เข้าหน้าอื่นไม่ได้**
- หลังจากนั้น `POST /api/auth/login` **ตอบ 403** ถ้าบัญชีนั้นมีพาสคีย์แล้ว
  (การซ่อนช่องรหัสผ่านใน UI ไม่ใช่การป้องกัน — server ปฏิเสธเอง)
- credential เป็นแบบ discoverable → login ไม่ต้องพิมพ์ชื่อผู้ใช้ และ endpoint
  ไม่บอกว่าบัญชีไหนมีจริง
- `userVerification: 'required'` → ต้องมีลายนิ้วมือ/ใบหน้า/PIN ไม่ใช่แค่แตะ
- **ทำอุปกรณ์หาย**: admin กด *รีเซ็ตพาสคีย์* ที่หน้าบัญชีคนนั้น → ลบพาสคีย์ทุกอัน
  ตัดทุก session และออกรหัสผ่านชั่วคราวให้อ่านครั้งเดียว
  (ข้อนี้สำคัญ — องค์กรที่ไม่มีทางกลับเข้าระบบคือองค์กรที่เลิกใช้พาสคีย์ในเดือนที่สอง)

## 4. Rate limit ที่ไม่กลายเป็นอาวุธเสียเอง

**เดิม**: นับต่อ username อย่างเดียว 5 ครั้ง/15 นาที · ชื่อผู้ใช้เดาได้ (`admin`, `staff`)
ใครก็เขียนสคริปต์ยิงรหัสผิดใส่ `admin` ทุก 15 นาที แล้ว**แอดมินตัวจริงเข้าระบบไม่ได้ตลอดกาล**

**ตอนนี้** สาม bucket: คู่ (username, IP) · ต่อ IP · ต่อ username ทั้งโลก (ตั้งสูงมาก
เป็นแค่ backstop กัน distributed guess) · นับ**เฉพาะครั้งที่ล้มเหลว** สำเร็จแล้ว reset ทิ้ง
· พาสคีย์ใช้ bucket แยกที่หลวมกว่ามาก เพราะทั้งสำนักงานออกเน็ตทาง IP เดียวกัน
และพาสคีย์ไม่มี brute force ให้หน่วงอยู่แล้ว
· limiter **fail open** โดยตั้งใจ — ถ้าตัว limiter เองพัง ต้องไม่ล็อกทั้งองค์กรออกจากระบบ

## 5. ดึงข้อมูลทั้งฐานไม่ได้ในคำขอเดียว

endpoint ที่แบ่งหน้าทั้งหมด cap `limit` ไว้ที่ 100 · ขอเกินก็ได้แค่ 100
· ไม่ส่ง `limit` ยังคืนทั้งหมดเหมือนเดิม เพราะ dropdown เลือกอุปกรณ์กับ dashboard ต้องการครบ
แต่นั่นเป็น endpoint ที่ไม่มี PII

## 6. Audit log บันทึกการ "เปิดดู" ไม่ใช่แค่การ "แก้"

`logRead()` ใน `lib/audit.ts` เขียน action `read_personal_data` ทุกครั้งที่มีคนเปิดข้อมูลผู้ยืม
หรือเปิดรูปบัตร · เขียนแบบ fire-and-forget เพื่อไม่ให้การ log ที่ล้มเหลวทำให้หน้าเปิดไม่ขึ้น
· PDPA ให้สิทธิ์เจ้าของข้อมูลถามว่า "ใครดูข้อมูลฉันบ้าง" — ถ้าไม่บันทึกก็ตอบไม่ได้
· และถ้าวันหนึ่งมีข้อมูลหลุด นี่คือสิ่งเดียวที่บอกได้ว่าหลุดไปเท่าไหร่

## 7. Security headers + CSP

อยู่ที่ `next.config.mjs` · `Content-Security-Policy`, `Strict-Transport-Security`,
`X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `X-Content-Type-Options`
· `poweredByHeader: false`
· **`'unsafe-eval'` เปิดเฉพาะตอน dev** เพราะ hot reload ของ Next ต้องใช้ · production ไม่มี
· หน้าที่มี PII ตั้ง `Cache-Control: no-store` และ `X-Robots-Tag: noindex`

## 8. อัปโหลดรูป

- **พิสูจน์ว่าเป็นรูปจริง** โดยให้ `sharp` decode แล้ว re-encode ใหม่ ไม่ได้เชื่อ MIME type
  ที่ browser ส่งมา (ไฟล์ HTML ที่ตั้งชื่อว่า `.jpg` ถูกปฏิเสธ)
- **การ re-encode ลบ EXIF ทิ้งไปด้วย** ซึ่งสำคัญมาก: รูปถ่ายจากมือถือมี **พิกัด GPS**
  ฝังอยู่ แปลว่ารูปบัตรที่ถ่ายที่บ้านคนไข้ = ที่อยู่จริงของคนไข้ ติดไปกับไฟล์
- นามสกุลไฟล์มาจากผลการ decode ไม่ใช่ชื่อไฟล์ที่ผู้ใช้ตั้ง
- ย่อด้านยาวสุดเหลือ 2000px และเก็บเป็น `.webp` (ลดทั้ง bandwidth และโควตา Blob)

## 9. รูปข้อมูลสุขภาพเป็น private blob

`lib/storage.ts` เก็บทุกโฟลเดอร์ยกเว้น `equipment/` เป็น `access: 'private'`
· อ่านได้ผ่าน token ของ store เท่านั้น URL หลุดไปก็เปิดไม่ได้
· เสิร์ฟผ่าน `/api/files/[...id]` ที่เช็ค session · รูปแคตตาล็อกอุปกรณ์ยัง public
เพราะขึ้นหน้าแรกสาธารณะ แต่ล็อก prefix ไว้ที่ `equipment/` เดินเข้าโฟลเดอร์อื่นไม่ได้

> ⚠️ **ถ้าเคย deploy เวอร์ชันก่อนหน้าไปแล้ว** ไฟล์เก่ายังเป็น public อยู่ —
> การเปลี่ยนโค้ดไม่ย้อนไปแก้ไฟล์ที่อัปโหลดไปแล้ว ต้องลบ store ทิ้งแล้วสร้างใหม่

## 10. Dependencies

`sharp` ถูกประกาศเป็น dependency ตรง ๆ (เดิมติดมาเป็น transitive ของ Next ที่เวอร์ชัน 0.34.5
ซึ่งมี CVE ของ libvips) และใช้ `overrides` ใน `package.json` บังคับให้ **ทั้ง tree**
ใช้ `sharp` กับ `postcss` เวอร์ชันที่แพตช์แล้ว — ไม่งั้น Next จะยังลากสำเนาเก่าของตัวเอง
ติดมาอยู่ดี ทั้งที่โค้ดเราเรียกตัวใหม่

**เช็คด้วย `npm audit` ก่อน deploy ทุกครั้ง** และดูว่าที่เหลือคืออะไร:
ช่องโหว่ของ **เครื่องมือตอน dev** (เช่น `esbuild` ที่ติดมากับ `drizzle-kit`) ไม่ได้ขึ้น
production เพราะเป็น devDependency และไม่มี dev server ของ esbuild รันจริง —
แต่ก็ยังควรอัปเมื่ออัปได้ อย่าปล่อยให้รายการยาวขึ้นจนไม่มีใครอ่าน

## 11. นโยบายรหัสผ่าน

`lib/password.ts` · อย่างน้อย 12 ตัว · ห้ามตัวอักษรเดียวกันทั้งหมด · ห้ามเรียงกัน
· ห้ามรหัสยอดฮิต · **ห้ามมีชื่อผู้ใช้หรือชื่อจริงอยู่ในนั้น** · เช็คที่ server ทุกทาง
ทั้งตอนแอดมินสร้างบัญชีและตอนเจ้าหน้าที่เปลี่ยนเอง

## 12. กันบอทที่ฟอร์มสาธารณะ — Cloudflare Turnstile

`/request` และ `/tracking` มี rate limit ต่อ IP อยู่แล้ว แต่ IP เปลี่ยนได้ถูกมาก
และสองเส้นทางนี้แพงกว่าที่ตาเห็น: ฟอร์มคำขอสร้างแถวผู้ยืมและกินโควตา Blob ทุกครั้ง
ส่วนหน้าติดตามคือ **ตัวถามคำถามเกี่ยวกับเลขบัตรประชาชน** ที่ถ้ากวาดมากพอก็ไล่ได้

**Turnstile ไม่ใช่ CAPTCHA** — ส่วนใหญ่ไม่ถามอะไรผู้ใช้เลย เว็บที่บังคับให้คนที่ยืนอยู่
ในโรงพยาบาลกดเลือกรูปสัญญาณไฟก่อนถึงจะยืมรถเข็นได้ คือเว็บที่แก้ปัญหาผิดข้อ

- **ปิดอยู่โดยปริยาย** — ไม่ใส่ key = ไม่มีอะไรเปลี่ยน ฟอร์มทำงานเหมือนเดิมทุกอย่าง
  ต้องใส่ทั้ง `NEXT_PUBLIC_TURNSTILE_SITE_KEY` และ `TURNSTILE_SECRET_KEY` (ดู `DEPLOY.md`)
  · `npm run check-env` จะบ่นถ้าใส่มาแค่ตัวเดียว
- **fail closed เมื่อ Cloudflare ตอบว่า token ใช้ไม่ได้** แต่ **fail open เมื่อติดต่อ
  Cloudflare ไม่ได้เลย** — ด้วยเหตุผลเดียวกับ rate limiter คือของเสียที่ผู้ให้บริการอื่น
  ต้องไม่ทำให้คนยืมกายอุปกรณ์ไม่ได้ · rate limit ยังทำงานอยู่ข้างล่างเสมอ
- token ใช้ได้ครั้งเดียว ระบบจึงรีเซ็ต widget ทุกครั้งที่กดส่ง — ไม่งั้นคนที่พิมพ์เบอร์ผิด
  แล้วแก้แล้วกดใหม่ จะโดนบอกว่า "การยืนยันหมดอายุ" ซึ่งงงและแก้ไม่ถูก
- CSP เปิดให้ `challenges.cloudflare.com` **เฉพาะเมื่อมี site key** เท่านั้น

**ถ้าโดนถล่มจริงตอนนี้เลย** ทางที่เร็วที่สุดคือเปิด **Vercel WAF → Attack Challenge Mode**
ซึ่งกดเปิด-ปิดได้ทันทีโดยไม่ต้อง deploy อะไรเลย

> ⚠️ **ยังไม่ได้เทสจริงครบวง** — ฝั่ง server เทสแล้วด้วย test key ของ Cloudflare
> ทั้งแบบ "ผ่านเสมอ" และ "ไม่ผ่านเสมอ" (ปฏิเสธถูกต้องทั้งคู่) และเทสว่าหน้าเว็บโหลดสคริปต์
> และล็อกปุ่มส่งไว้จริง · แต่ **ตัว widget ยังไม่เคยเรนเดอร์จริง** เพราะเบราว์เซอร์ในเครื่อง
> ที่พัฒนาออกเน็ตไป `challenges.cloudflare.com` ไม่ได้
> · **หลัง deploy ครั้งแรกที่ใส่ key ให้ลองส่งคำขอจริงหนึ่งใบด้วยตัวเอง** ก่อนประกาศใช้

## 13. ลบข้อมูลตามกำหนด (PDPA)

ประกาศความเป็นส่วนตัวใน `lib/consent.ts` **บอกผู้ใช้ไว้ว่าเก็บ 2 ปีหลังการยืมครั้งสุดท้าย
จากนั้นจะลบ** ก่อนหน้านี้ระบบทำตามไม่ได้ ซึ่งภายใต้ PDPA แย่กว่าการไม่ประกาศไว้เลย
เพราะประกาศคือสิ่งที่ผู้ใช้ให้ความยินยอมต่อ

- `lib/retention.ts` · Vercel Cron เรียก `/api/cron/retention` ทุกวันอาทิตย์ (ดู `vercel.json`)
- **ไม่ลบทั้งแถว** — ทุกรายการยืม คืน อนุมัติ และ audit log อ้างถึงรหัสผู้ยืม
  ลบแถวทิ้งจะทำให้ประวัติหลายปีกลายเป็นรหัสลอย ๆ และทำลายหลักฐานที่การตรวจสอบต้องใช้พอดี
  · **สิ่งที่ลบคือทุกอย่างที่ทำให้แถวนั้นเป็น "คน"**: ชื่อ เลขบัตร ที่อยู่ เบอร์ LINE อีเมล
  อาการป่วย และรูปทั้งหมด (ลบไฟล์จริงใน Blob ไม่ใช่แค่ตัดลิงก์)
  · เหลือไว้แค่รหัสผู้ยืม วันลงทะเบียน และประวัติการยืม — ซึ่งระบุตัวใครไม่ได้
- **ยกเว้นคนที่ยังยืมของอยู่** ต่อให้ลงทะเบียนมานานแค่ไหน · และนับจากกิจกรรมล่าสุดทุกชนิด
  (ยืม คำขอ หรือวันลงทะเบียนสำหรับคนที่ไม่เคยยืม)
- `anonymised_at` เป็นทั้งธงว่า "ทำไปแล้ว" และหลักฐานว่าทำเมื่อไหร่ · รันซ้ำได้ ไม่มีผลข้างเคียง
- **บันทึก audit ทุกครั้ง** ในชื่อ `ระบบอัตโนมัติ` (`system`) ไม่ใช่ `ผู้ใช้ทั่วไป` — การลบข้อมูล
  โดยไม่มีร่องรอยว่าเคยลบ คือสิ่งที่การทำตาม PDPA ต้องแสดงให้ได้พอดี

**ตรวจก่อนเชื่อ:**
```bash
npm run retention:preview       # บอกว่าใครจะถูกลบ ไม่แตะอะไรเลย
npm run retention:run -- --yes  # ลบจริง · ไม่ใส่ --yes = ได้แค่รายงาน
```
`--yes` คือขั้นที่สองตามกติกาของโปรเจกต์เรื่องการกระทำที่ย้อนกลับไม่ได้

---

## ยังไม่ได้ทำ

### 14. สิ่งที่ต้องตั้งเองใน Vercel / Neon (โค้ดทำให้ไม่ได้)

- [ ] **Vercel WAF** เปิด rate limit ที่ระดับ edge และเตรียม Attack Challenge Mode ไว้
- [ ] **Spend limit** ของ Vercel — ถ้าโดนถล่ม บิลจะพุ่ง ตั้งเพดานไว้
- [ ] **Deployment Protection** ปิด preview deployment ไม่ให้คนนอกเข้า
      (preview ชี้ฐานข้อมูลเดียวกัน = ประตูหลังที่ไม่มีใครดู)
- [ ] **สร้าง DB role แยกสำหรับแอป** ที่ไม่ใช่ owner — ตอนนี้แอปต่อด้วยสิทธิ์เต็ม
      SQL injection สักจุดเดียวจะ drop ตารางได้
- [ ] **สำรอง Blob** — `pg_dump` ไม่รวมรูป
- [ ] **แจ้งเตือนจาก audit log** อย่างน้อยให้มีคนดู `read_personal_data` เป็นระยะ
      ว่ามีใครเปิดดูข้อมูลผู้ยืมรัวผิดปกติไหม
- [ ] **ตั้ง `CRON_SECRET`** ไม่งั้น `/api/cron/retention` จะรับเฉพาะ admin ที่ login อยู่
      และ cron รายสัปดาห์จะทำงานไม่ได้ (Vercel สร้างให้อัตโนมัติเมื่อเพิ่ม cron แต่ตรวจดูด้วย)
- [ ] **ตั้ง `WEBAUTHN_RP_ID`** เป็นโดเมนจริง เพื่อตรึงค่าไว้ (ดู `DEPLOY.md`)
- [ ] **เก็บ `ENCRYPTION_KEY` สำรองไว้นอก Vercel** — ทำหายคือเลขบัตรทุกคนอ่านไม่ออกถาวร

---

## ถ้าเกิดข้อมูลหลุดจริง

PDPA มาตรา 37(4) กำหนดให้**แจ้งสำนักงาน กคส. ภายใน 72 ชั่วโมง** นับแต่ทราบเหตุ
และถ้ามีความเสี่ยงสูงต่อเจ้าของข้อมูล ต้องแจ้งเจ้าของข้อมูลด้วย
ลำดับที่ควรทำ: ตัด session ทั้งหมด (ดัน `sessionVersion` ของทุกบัญชี) → เปลี่ยน
`SESSION_SECRET` → ดู audit log ว่าถูกเปิดดูอะไรไปบ้าง → แจ้งตามกฎหมาย
