# ส่งต่อโปรเจกต์ให้คนอื่นดูแล

คู่มือย้ายระบบนี้ไปอยู่ในบัญชีของอีกคน · เขียนไว้ตอนทั้งสองฝั่งใช้ Vercel

> อ่าน `DEPLOY.md` ก่อนถ้ายังไม่เคยตั้งระบบนี้มาก่อน ไฟล์นี้พูดถึง**การย้าย**อย่างเดียว

---

## ระบบนี้ประกอบด้วยอะไรบ้าง

ย้ายแค่โค้ดไม่พอ มีของ 5 ชิ้นที่ต้องไปด้วยกัน:

| ชิ้น | อยู่ที่ไหน | ถ้าลืม |
|---|---|---|
| โค้ด | GitHub | deploy ไม่ได้ |
| เว็บ | Vercel project | ไม่มีที่รัน |
| ฐานข้อมูล | Neon | ข้อมูลผู้ยืม/ประวัติหายหมด |
| รูปภาพ | Vercel Blob | **รูปบัตร ปชช. หายหมด** — dump ฐานข้อมูลไม่มีรูปติดไปด้วย |
| ความลับ | Environment Variables | ดูตารางข้างล่าง มีตัวที่ทำหายแล้วจบเลย |

### ตัวแปรลับ — ตัวไหนก๊อปได้ ตัวไหนสร้างใหม่ได้

| ตัวแปร | ย้ายยังไง |
|---|---|
| `ENCRYPTION_KEY` | **ต้องก๊อปตัวเดิมเท่านั้น ห้ามสร้างใหม่** เป็นกุญแจดอกเดียวที่ถอดรหัสเลขบัตรประชาชนได้ · สร้างใหม่ = เลขบัตรของผู้ยืมทุกคนอ่านไม่ออกถาวร กู้ไม่ได้ไม่ว่าวิธีใด |
| `DATABASE_URL` | ได้มาใหม่จาก Neon ของปลายทาง |
| `SESSION_SECRET` | สร้างใหม่ได้ (`openssl rand -base64 48`) · ผลคือทุกคนถูกเด้งออกจากระบบ ต้องล็อกอินใหม่ เท่านั้น |
| `CRON_SECRET` | สร้างใหม่ได้ · Vercel มักใส่ให้เองเมื่อมี cron |
| `BLOB_STORE_ID` | Vercel ใส่ให้เองตอน connect Blob store |
| `WEBAUTHN_RP_ID` | ถ้าตั้งไว้ ต้องแก้ให้ตรงโดเมนใหม่ · **อ่านหัวข้อ "กับดัก" ก่อน** |
| `NEXT_PUBLIC_TURNSTILE_SITE_KEY` / `TURNSTILE_SECRET_KEY` | ถ้าใช้ ต้องไปเพิ่มโดเมนใหม่ใน Cloudflare Turnstile ด้วย |

---

## ⚠️ กับดักที่ต้องรู้ก่อนเริ่ม

### 1. เปลี่ยนโดเมน = พาสคีย์ของทุกคนใช้ไม่ได้ทันที

พาสคีย์ผูกกับ**ชื่อโดเมน**โดยตัวมาตรฐานเอง ไม่ใช่แค่การตั้งค่า — พาสคีย์ที่สร้างบน
`เว็บเก่า.vercel.app` ใช้บน `เว็บใหม่.vercel.app` **ไม่ได้เด็ดขาด** และแก้ที่ฝั่งเราไม่ได้

แล้วมันแย่กว่านั้น เพราะระบบตั้งใจให้ **บัญชีที่มีพาสคีย์แล้ว ล็อกอินด้วยรหัสผ่านไม่ได้**
(กันฟิชชิ่ง) และปุ่ม "รีเซ็ตพาสคีย์" อยู่หลังการล็อกอินเป็น admin

**= ถ้าย้ายโดเมนเฉย ๆ จะไม่มีใครเข้าระบบได้เลย รวมทั้ง admin**

**ทางออก** เข้า Neon SQL Editor ของฐานข้อมูลปลายทาง แล้วรัน:

```sql
-- ลบพาสคีย์ทั้งหมด เพื่อให้ทุกคนกลับไปล็อกอินด้วยรหัสผ่านได้อีกครั้ง
-- (เข้าไปแล้วระบบจะบังคับให้สร้างพาสคีย์ใหม่บนโดเมนใหม่ทันที)
DELETE FROM passkeys;
```

ทำ **หลัง** เว็บโดเมนใหม่ขึ้นเรียบร้อยแล้ว แล้วบอกเจ้าหน้าที่ทุกคนว่าให้ล็อกอิน
ด้วยรหัสผ่านที่ admin ให้ แล้วสร้างพาสคีย์ใหม่

> ถ้า**ใช้โดเมนเดิม** (เช่นย้าย custom domain ตามไปด้วย) ข้อนี้ข้ามได้ พาสคีย์ยังใช้ได้ปกติ

### 2. รูปภาพไม่ได้อยู่ในฐานข้อมูล

`pg_dump` ได้แค่ตาราง · ในตารางเก็บแค่ "ชื่อไฟล์" ส่วนไฟล์จริงอยู่ใน Vercel Blob
ถ้าย้ายฐานข้อมูลอย่างเดียว หน้าคำขอจะขึ้นว่า "เปิดรูปไม่ได้ — ไฟล์อาจถูกลบไปแล้ว"

### 3. PDPA — ผู้ควบคุมข้อมูลเปลี่ยนตัว

ผู้ยืมให้ความยินยอมกับ**ผู้ดำเนินการเดิม** การเปลี่ยนคนดูแลข้อมูลสุขภาพและเลขบัตร
ประชาชนของประชาชนไม่ใช่เรื่องเทคนิคล้วน ๆ · ถ้าเป็นการเปลี่ยนมือจริงจัง
ควรปรึกษาที่ปรึกษากฎหมายของมูลนิธิ และพิจารณาว่าต้องแจ้งเจ้าของข้อมูลหรือไม่
· ถ้าแค่เปลี่ยนคนดูแลเทคนิคภายในองค์กรเดิม ข้อนี้ไม่เกี่ยว

---

## ล้างข้อมูลก่อนส่งต่อ (ถ้าไม่ต้องการให้ข้อมูลเดิมติดไปด้วย)

ถ้าที่ผ่านมาเป็นแค่การทดลอง หรือไม่อยากให้ข้อมูลผู้ยืมจริงไปอยู่ในมือคนอื่น
ต้องล้าง **สองที่** ไม่ใช่ที่เดียว:

```bash
# 1. รูปภาพใน Vercel Blob — SQL ล้างให้ไม่ได้
npm run wipe-blob              # บอกว่ามีกี่ไฟล์ ยังไม่ลบ
npm run wipe-blob -- --yes     # ลบจริง

# 2. ฐานข้อมูล — เอา sql/reset.sql ไปวางใน Neon SQL Editor
```

`sql/reset.sql` มีสองแบบให้เลือก (อย่ารันทั้งคู่):

| แบบ | ลบอะไร | เหลืออะไร | เหมาะกับ |
|---|---|---|---|
| **A** | ผู้ยืม ประวัติยืม-คืน คำขอ audit พาสคีย์ | ตาราง บัญชีเจ้าหน้าที่ รายการอุปกรณ์ | ส่งต่อระบบที่พร้อมใช้ทันที |
| **B** | ทุกอย่าง เหลือฐานเปล่า | ไม่เหลืออะไรเลย | ให้เพื่อนเริ่มจากศูนย์ ตั้งรหัสเอง (ต้องรัน `sql/setup.sql` ต่อ) |

แบบ A รีเซ็ตเลขลำดับให้ด้วย ผู้ยืมคนแรกของเพื่อนจะได้รหัส `B0001` ไม่ใช่ต่อจากของเก่า
· คืนยอดสต็อกให้เต็ม (ของที่ยังยืมค้างอยู่ตอนล้าง ไม่งั้นสต็อกจะขาดโดยไม่มีรายการอธิบาย)
· และลบพาสคีย์ทั้งหมด ซึ่ง**จำเป็น** ถ้าเพื่อนใช้โดเมนใหม่ — ดูหัวข้อกับดักข้างบน

> **สำรองก่อนเสมอ** `pg_dump "$DATABASE_URL" -Fc -f backup.dump` หรือใช้ branch/restore ของ Neon
> · และอย่ารันตอนที่ยังมีคนใช้เว็บอยู่ เว็บจะพังทันที

---

## ทางเลือกที่ 1 — โอนของเดิมทั้งชุด (แนะนำถ้าใช้งานจริงอยู่แล้ว)

เก็บข้อมูล ประวัติ และรูปไว้ครบ ไม่ต้องย้ายอะไร

1. **GitHub** — Settings → Transfer ownership ไปให้บัญชีเพื่อน
   (หรือถ้าอยากเก็บ repo ไว้เอง แค่เพิ่มเขาเป็น collaborator ก็พอ)
2. **Vercel** — Project Settings → Transfer โอน project ไปยัง account/team ของเพื่อน
   · Vercel จะให้เลือกว่า storage ที่ผูกอยู่จะไปด้วยไหม อ่านหน้าจอตรงนั้นให้ดี
3. **Neon + Blob** — ถ้าโอนไปพร้อม project แล้วไม่ต้องทำอะไรต่อ
   ถ้าไม่ ให้ทำตามทางเลือกที่ 2 เฉพาะส่วนข้อมูล
4. **ตรวจ Environment Variables** ให้ครบทุกตัวในตารางข้างบน โดยเฉพาะ `ENCRYPTION_KEY`
5. **Redeploy หนึ่งครั้ง** ให้ env ใหม่มีผล
6. ทำ **เช็กลิสต์หลังย้าย** ท้ายไฟล์

**โดเมนไม่เปลี่ยน → พาสคีย์ไม่พัง** นี่คือข้อดีหลักของทางนี้

---

## ทางเลือกที่ 2 — เพื่อน deploy ชุดใหม่ของตัวเอง

เหมาะกับตอนยังไม่มีข้อมูลจริง หรืออยากเริ่มสะอาด ๆ

### ก. โค้ดกับเว็บ

1. เพื่อน fork repo บน GitHub (หรือรับสิทธิ์เข้า repo เดิม)
2. Vercel → Add New → Project → เลือก repo นั้น
   · **Framework Preset ต้องเป็น Next.js** ถ้าเป็น `Other` ทุกหน้าจะขึ้น 404
3. Storage → Create Database → **Neon** → region **Singapore**
4. Storage → Create → **Blob** → กด Connect ทั้งคู่เข้ากับ project
   · เห็นแค่ `BLOB_STORE_ID` ไม่มี token = ปกติแล้ว (Vercel ใช้ OIDC)

### ข. ฐานข้อมูล — วิธีง่ายสุด ไม่ต้องลงอะไรเลย

เปิด **Neon Console → SQL Editor** แล้วเอาไฟล์ **`sql/setup.sql`** ในโปรเจกต์นี้
วางลงไปทั้งไฟล์ กด Run

ไฟล์เดียวจบ: สร้างตารางครบทั้ง 11 ตาราง สร้างบัญชี `admin`/`staff` และใส่อุปกรณ์
ตัวอย่างให้ 5 รายการ

- **ต้องแก้รหัสผ่านสองบรรทัดก่อนรัน** (ค้นหาคำว่า `REPLACE-THIS` ในไฟล์)
  ถ้าไม่แก้ ไฟล์จะหยุดและแจ้ง error ให้ ไม่สร้างบัญชีที่ใช้รหัสตัวอย่าง
- รันซ้ำได้ปลอดภัย ไม่ทับข้อมูลเดิม ไม่รีเซ็ตรหัสผ่านใคร
- บันทึกลงตาราง `_migrations` ให้ด้วย เพราะฉะนั้นถ้าวันหลังมีคนรัน
  `npm run db:migrate` จากเครื่องตัวเอง มันจะรู้ว่ารันไปแล้วและข้ามให้

> ถ้าถนัด command line จะใช้ `npm run db:migrate && npm run seed` แทนก็ได้ ผลเหมือนกันเป๊ะ
> (ตรวจแล้วด้วยการ diff schema ทั้งสองวิธี — เหมือนกันทุกคอลัมน์ ทุก index ทุก constraint)

### ค. ตัวแปรลับ

Vercel → Settings → Environment Variables ใส่ให้ครบทุก environment
(Production / Preview / Development):

```
SESSION_SECRET      openssl rand -base64 48
ENCRYPTION_KEY      openssl rand -base64 32     ← ถ้าย้ายข้อมูลเก่ามาด้วย ต้องใช้ตัวเดิม!
CRON_SECRET         openssl rand -base64 32
```

**เก็บ `ENCRYPTION_KEY` สำรองไว้นอก Vercel ด้วย** (password manager ขององค์กร
หรือปริ้นเก็บในตู้เซฟ) ทำหายคือจบ

### ง. ย้ายข้อมูลเก่ามาด้วย (ถ้าต้องการ)

```bash
# จากฐานข้อมูลเดิม
pg_dump "$OLD_DATABASE_URL" -Fc -f medequip.dump

# เข้าฐานข้อมูลใหม่ (ยังไม่ต้องรัน setup.sql ถ้าจะ restore ทับ)
pg_restore -d "$NEW_DATABASE_URL" --no-owner --no-privileges medequip.dump
```

แล้ว **ต้องเอา `ENCRYPTION_KEY` ตัวเดิมมาด้วย** ไม่งั้นเลขบัตรอ่านไม่ออกทั้งฐาน

**รูปภาพต้องย้ายแยก** — ไฟล์อยู่ใน Blob store เก่า ไม่ได้อยู่ใน dump
ถ้าย้าย Blob store ไม่ได้ ทางที่ตรงไปตรงมาคือให้ผู้ยืมส่งรูปใหม่

---

## เช็กลิสต์หลังย้าย

ไล่ทีละข้อ อย่าข้าม:

- [ ] เปิดหน้าแรก — รายการอุปกรณ์ขึ้นครบ
- [ ] `/request` ส่งคำขอทดสอบหนึ่งใบ **พร้อมแนบรูปบัตร** → ต้องได้รหัสคำขอกลับมา
- [ ] เข้า `/staff` ด้วย `admin` + รหัสที่ตั้งไว้ → ต้องเจอหน้า **บังคับสร้างพาสคีย์**
- [ ] สร้างพาสคีย์ → เข้าแอปได้
- [ ] ลองล็อกอินด้วยรหัสผ่านเดิมอีกครั้ง → **ต้องโดนปฏิเสธ (403)** นั่นคือทำงานถูก
- [ ] เปิดคำขอทดสอบเมื่อกี้ → **เห็นรูปบัตรที่แนบมา**
- [ ] **ทิ้งไว้ ~30 นาที แล้วเปิดรูปนั้นซ้ำ** — ถ้ายังเปิดได้ แปลว่า Blob ทำงานจริง
      ถ้าขึ้น "เปิดรูปไม่ได้" แปลว่ารูปตกไปอยู่ดิสก์ชั่วคราว = ยังไม่ได้ connect Blob
- [ ] อัปรูปอุปกรณ์หนึ่งรูปในหน้าสต็อก → ต้องขึ้นทั้งข้างรายการและบนหน้าแรก
- [ ] `/tracking` ค้นด้วยเลขบัตรที่เพิ่งใช้ → เจอคำขอ
- [ ] หน้า **ประวัติ → Audit Log** (เห็นเฉพาะ admin) มีรายการขึ้นจริง
- [ ] ลบคำขอ/ผู้ยืมทดสอบทิ้ง แล้วเปลี่ยนรหัสผ่าน `staff` ที่ยังไม่ได้ใช้
- [ ] อ่าน `SECURITY.md` หัวข้อ 14 — รายการที่ต้องไปตั้งเองใน Vercel/Neon
      (WAF, spend limit, ปิด preview deployment, สำรอง Blob)

---

## เอกสารอื่นในโปรเจกต์

| ไฟล์ | เรื่อง |
|---|---|
| `README.md` | ระบบทำอะไรได้บ้าง โครงสร้างโปรเจกต์ |
| `DEPLOY.md` | ตั้งค่าตั้งแต่ต้น · ตัวแปรทั้งหมด · พาสคีย์ · แก้ปัญหาที่เจอบ่อย |
| `SECURITY.md` | ผลตรวจความปลอดภัย 14 ข้อ และสิ่งที่ยังต้องทำเอง |
| `CLAUDE.md` | กติกาสำหรับคนเขียนโค้ดต่อ — อ่านก่อนแก้อะไร |
| `sql/setup.sql` | ติดตั้งฐานข้อมูลไฟล์เดียว สำหรับ SQL editor |
| `sql/reset.sql` | ล้างข้อมูลก่อนส่งต่อ (สองแบบ) |
