# คู่มือติดตั้งและ deploy

ระบบนี้ deploy บน **Vercel** และใช้ **PostgreSQL** (Neon) กับ **Vercel Blob** สำหรับเก็บรูป
ไม่ต้องเช่า VPS ไม่ต้อง SSH ไม่ต้องดูแลเซิร์ฟเวอร์เอง

> เวอร์ชันแรกของระบบเป็น Express + ไฟล์ JSON บน VPS ตอนนี้ย้ายมา Next.js + PostgreSQL แล้ว
> คู่มือ VPS เดิมไม่ใช้แล้ว

---

## ก่อนเริ่ม: ใครควรเป็นเจ้าของบัญชี

**ให้เจ้าของระบบตัวจริง (มูลนิธิ หรือคนที่จะดูแลระยะยาว) เป็นคนสมัคร Vercel เอง**
แล้ว invite ผู้พัฒนาเข้าไปเป็น Member — อย่าให้ผู้พัฒนาสมัครแล้วค่อยโอนทีหลัง เพราะ:

- ค่าใช้จ่ายรายเดือนจะไปลงบัตรของผู้พัฒนา
- **ข้อมูลผู้ป่วยจะอยู่ในบัญชีส่วนตัวของผู้พัฒนาในทางกฎหมาย**
- Blob store ผูกกับบัญชี ไม่ใช่ project — ย้าย project ข้ามบัญชี **รูปที่อัปโหลดไว้ไม่ตามไปด้วย**

ถ้าจำเป็นต้องสร้างในบัญชีผู้พัฒนาไปก่อน **ต้องย้ายให้เสร็จก่อนเจ้าหน้าที่กรอกข้อมูลคนไข้คนแรก**
หลังจากนั้นการย้ายจะกลายเป็นงาน migration ที่มีความเสี่ยงข้อมูลสูญหาย

---

## ขั้นตอนที่ 1: สร้าง Vercel project

1. [vercel.com/new](https://vercel.com/new) → Import repo นี้จาก GitHub
2. ตรวจว่า **Framework Preset ขึ้นเป็น `Next.js`** โดยอัตโนมัติ
3. ช่อง **Build Command / Output Directory / Install Command / Root Directory ปล่อยว่างทั้งหมด**

> ⚠️ **จุดที่พลาดกันบ่อยที่สุด** — ถ้า Framework Preset เป็น `Other` แอปจะ build ผ่านแต่เปิดเว็บแล้วขึ้น
> `404: NOT_FOUND` ทุกหน้า เพราะ Vercel ไม่ได้รัน `next build` แล้วไปเสิร์ฟโฟลเดอร์ `public/`
> เป็นไฟล์นิ่ง ๆ แทน ซึ่งไม่มี `index.html` อยู่
>
> Preset นี้ **ไม่อัปเดตเองเมื่อโค้ดเปลี่ยน** ถ้า project เคยถูกสร้างตอนที่ repo ยังเป็นระบบเก่า
> ต้องไปแก้เองที่ Settings → General → Framework Preset แล้ว **Redeploy**
> วิธีเช็คเร็วสุด: เปิด build log แล้วหาคำว่า `next build` ถ้าไม่เจอคือ preset ผิด

---

## ขั้นตอนที่ 2: สร้างฐานข้อมูลและที่เก็บรูป

ทำ **จากในหน้า Vercel** ทั้งคู่ เพื่อให้ทุกอย่างอยู่ใต้บัญชีเดียวและไม่ต้องโอนทีหลัง

| | ทำอะไร |
|---|---|
| ฐานข้อมูล | Storage → Create Database → **Neon** → เลือก region **Singapore** |
| ที่เก็บรูป | Storage → Create → **Blob** |

แล้วกด **Connect** ทั้งสองตัวเข้ากับ project — Vercel จะใส่ `DATABASE_URL` และ
`BLOB_READ_WRITE_TOKEN` ให้อัตโนมัติ

**ทำไมต้อง Singapore**: `vercel.json` ตรึง region ไว้ที่ `sin1` ซึ่งใกล้ประเทศไทยที่สุดเท่าที่ Vercel มี
ถ้าฐานข้อมูลอยู่คนละทวีป ทุก query จะวิ่งข้ามโลกและช้าขึ้นหลายเท่า

**ใช้ connection string แบบ Pooled** (มี `-pooler` ในชื่อ host) — แต่ละ serverless invocation
เปิด connection ของตัวเอง ถ้าใช้แบบ unpooled พอมีคนใช้พร้อมกัน connection จะเต็มแล้วระบบล่มทั้งระบบ

---

## ขั้นตอนที่ 3: ตั้งค่าความลับ

Settings → Environment Variables ใส่ 4 ตัวนี้ (ให้ครบทั้ง Production / Preview / Development)

| ตัวแปร | สร้างด้วย |
|---|---|
| `SESSION_SECRET` | `openssl rand -base64 48` |
| `ENCRYPTION_KEY` | `openssl rand -base64 32` |
| `SEED_ADMIN_PASSWORD` | รหัสผ่านแอดมิน (อย่างน้อย 8 ตัวอักษร) |
| `SEED_STAFF_PASSWORD` | รหัสผ่านเจ้าหน้าที่ |

> 🔑 **`ENCRYPTION_KEY` คือกุญแจดอกเดียวที่ถอดรหัสเลขบัตรประชาชนได้**
> ทำหายเมื่อไหร่ = เลขบัตรของผู้ยืมทุกคนอ่านไม่ออกถาวร กู้คืนไม่ได้ไม่ว่าวิธีใด
> **เก็บสำรองไว้นอก Vercel ด้วย** (password manager ของมูลนิธิ หรือปริ้นเก็บในตู้เซฟ)
> และห้ามเปลี่ยนหลังจากมีข้อมูลจริงแล้ว

ระบบตั้งใจให้ **ไม่ยอมทำงานถ้าขาด `SESSION_SECRET` หรือ `ENCRYPTION_KEY`** — ดีกว่าปล่อยให้รัน
ด้วยค่าเริ่มต้นที่ใครก็เดาได้ แล้วปลอม cookie เข้ามาเป็นแอดมิน

---

## ขั้นตอนที่ 4: สร้างตารางและข้อมูลเริ่มต้น

รันจากเครื่องตัวเอง โดยเอา connection string จาก Neon มาใส่ `.env.local`

```bash
git clone <repo> && cd medequiploan
npm install
cp .env.example .env.local     # แล้วแก้ค่าให้ครบ
npm run check-env              # ตรวจว่าตั้งครบและถูกต้องก่อน
npm run db:migrate             # สร้างตาราง
npm run seed                   # สร้างผู้ใช้ admin/staff + อุปกรณ์ตัวอย่าง
```

`db:migrate` รันซ้ำได้ปลอดภัย — มีตาราง `_migrations` จำว่ารันไฟล์ไหนไปแล้ว
และไม่ต้องมี `psql` ในเครื่อง (macOS ส่วนใหญ่ไม่มีติดมา)

---

## ขั้นตอนที่ 5: เข้าใช้งาน

| หน้า | URL | ต้อง login |
|---|---|---|
| หน้าแรก | `/` | ไม่ต้อง |
| ฟอร์มคำขอยืม (สำหรับผู้ป่วย/ผู้ดูแล) | `/request` | ไม่ต้อง |
| ระบบเจ้าหน้าที่ | `/staff` | ต้อง |

เข้า `/staff` ด้วย username **`admin`** และรหัสผ่านที่ตั้งไว้ใน `SEED_ADMIN_PASSWORD`
(username เป็นค่าตายตัว มีแค่ `admin` กับ `staff`)

**เปลี่ยนรหัสผ่านทันทีหลังใช้งานจริง** — ตอนนี้ยังไม่มีหน้าเปลี่ยนรหัสในระบบ ต้องทำผ่าน SQL:

```bash
node -e "console.log(require('bcryptjs').hashSync('รหัสใหม่', 10))"
psql "$DATABASE_URL" -c "UPDATE users SET password_hash='<hash ที่ได้>' WHERE username='admin';"
```

---

## อัปเดตโค้ดในอนาคต

`git push` → Vercel deploy ให้เอง

ถ้ามีการแก้ schema ต้องรัน `npm run db:migrate` **ก่อน** โค้ดใหม่ขึ้น production
ไม่งั้นโค้ดจะไปเรียกตารางหรือคอลัมน์ที่ยังไม่มี

---

## เรื่องที่ต้องรู้ก่อนใช้กับข้อมูลผู้ป่วยจริง

**ข้อมูลอยู่นอกประเทศไทย** — Vercel (สิงคโปร์) และ Neon (สิงคโปร์) ไม่มี data center ในไทย
PDPA ไม่ได้ห้ามส่งข้อมูลออกนอกประเทศเด็ดขาด (มาตรา 28 มีเงื่อนไขให้ทำได้) แต่ถ้ามูลนิธิมีนโยบาย
ว่าข้อมูลสุขภาพต้องอยู่ในไทย ต้องย้ายไป **AWS RDS PostgreSQL region `ap-southeast-7` (Bangkok)**
ซึ่งเป็นแค่การเปลี่ยน connection string — โค้ดไม่ต้องแก้ แต่ค่าใช้จ่ายสูงขึ้นราว 600-900 บาท/เดือน

**นี่คือการตัดสินใจของเจ้าของข้อมูล ไม่ใช่ของผู้พัฒนา** — แจ้งให้มูลนิธิรับทราบและตัดสินใจเอง
และถ้าจะใช้กับผู้ป่วยจริงจัง ควรปรึกษาที่ปรึกษากฎหมายของมูลนิธิ

**ค่าใช้จ่าย** — Vercel Hobby (ฟรี) มีเงื่อนไขห้ามใช้เชิงพาณิชย์ ระบบที่ให้บริการจริงต้องใช้
**Vercel Pro (~$20/เดือน)** ส่วน Neon และ Blob มี free tier ที่พอสำหรับองค์กรขนาดเล็ก
แต่ควรดูโควตาเป็นระยะ โดยเฉพาะพื้นที่เก็บรูป

---

## สำรองข้อมูล

Neon มี point-in-time restore ให้อยู่แล้ว (ระยะเวลาย้อนหลังขึ้นกับ plan) แต่ควรทำ dump เก็บเองด้วย:

```bash
pg_dump "$DATABASE_URL" -Fc -f medequip-$(date +%F).dump
```

**รูปภาพใน Blob ไม่ได้อยู่ใน dump นี้** ต้องสำรองแยกถ้าต้องการ

---

## แก้ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ |
|---|---|
| ทุกหน้าขึ้น `404: NOT_FOUND` ทั้งที่ build ผ่าน | Framework Preset เป็น `Other` — ดูขั้นตอนที่ 1 |
| `Error: ไม่พบ connection string` ตอนรัน script | ยังไม่ได้สร้าง `.env.local` หรือใส่ `DATABASE_URL` ไม่ครบ |
| `relation "..." does not exist` | ยังไม่ได้รัน `npm run db:migrate` |
| login ไม่ผ่านทั้งที่รหัสถูก | ยังไม่ได้รัน `npm run seed` — ยังไม่มีผู้ใช้ในระบบ |
| รูปที่อัปโหลดหายหลังผ่านไปสักพัก | ยังไม่ได้ connect Blob store — รูปตกไปอยู่ดิสก์ชั่วคราวซึ่งหายทุก cold start |
| connection ฐานข้อมูลเต็มตอนคนใช้เยอะ | ใช้ connection string แบบ unpooled — เปลี่ยนเป็นแบบที่มี `-pooler` |
